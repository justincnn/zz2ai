version: "3.9"

services:
  zz2ai:
    image: "${DOCKERHUB_USERNAME:-your_dockerhub_username}/zz2ai:latest"
    container_name: zz2ai
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
